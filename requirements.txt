numpy>=1.24
py-cpuinfo

# NVIDIA GPU (cupy + CUDA runtime libraries)
cupy-cuda12x
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
nvidia-curand-cu12

# Intel / AMD GPU
pyopencl

# Optional: prettier terminal output
rich>=13.0
